package com.example;

public class comentarios {
    /*
    inscripcion
    así se una inscripción:

    Inscripcion ins = new Inscripcion(
    1,
    usuario,
    curso,
    new Date(System.currentTimeMillis()),
    Inscripcion.EstadoInscripcion.ACTIVA):

    usuario.agregarInscripcion(ins);


    modulo y leccion
    Modulo m1 = new Modulo(1, "Intro", "Fundamentos", 1, curso);
    Leccion l1 = new Leccion(1, "¿Qué es Java?", "Intro básica", "url", 10, 1, m1);

    m1.agregarLeccion(l1);
    curso.agregarModulo(m1);
     */
    
}
